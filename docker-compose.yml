version: "3"
services:
  app:
    image: beginor/ng-esri-demo
    container_name: ng-esri-demo
    hostname: ng-esri-demo
    ports:
      - 1811:80
    # volumes:
    #   - ./default.conf:/etc/nginx/conf.d/default.conf
    #   - ./html:/usr/share/nginx/html
    #   - ./log:/var/log/nginx
    restart: unless-stopped
networks:
  default:
    external:
      name: compose
